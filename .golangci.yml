version: "2"

run:
  timeout: 5m
  exclude-dirs:
    - vendor
    - node_modules
    - dist

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - ineffassign

issues:
  max-issues-per-linter: 50
  max-same-issues: 5
