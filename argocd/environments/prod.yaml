apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grainguard-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: grainguard
  source:
    repoURL: https://github.com/pahuldeepp/GrainGuard-.git
    targetRevision: HEAD
    path: k8s/helm/grainguard
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
      parameters:
        - name: gateway.image.tag
          value: "latest"
        - name: bff.image.tag
          value: "latest"
        - name: ingestService.image.tag
          value: "latest"
  destination:
    server: https://kubernetes.default.svc
    namespace: grainguard-prod
  syncPolicy:
    # Production requires manual sync — no automated deploys
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
  revisionHistoryLimit: 10
