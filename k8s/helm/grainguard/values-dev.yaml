# Dev environment overrides — lower replicas, no autoscaling
image:
  tag: latest

services:
  gateway:
    replicaCount: 1
    autoscaling:
      enabled: false

  bff:
    replicaCount: 1
    autoscaling:
      enabled: false

  telemetry-service:
    replicaCount: 1
    autoscaling:
      enabled: false

  saga-orchestrator:
    replicaCount: 1
    autoscaling:
      enabled: false

  read-model-builder:
    replicaCount: 1
    autoscaling:
      enabled: false

  asset-registry:
    replicaCount: 1
    autoscaling:
      enabled: false

  risk-engine:
    replicaCount: 1
    autoscaling:
      enabled: false

  search-indexer:
    replicaCount: 1
    autoscaling:
      enabled: false

  workflow-alerts:
    replicaCount: 1
    autoscaling:
      enabled: false

  jobs-worker:
    replicaCount: 1
    autoscaling:
      enabled: false

  cassandra-writer:
    replicaCount: 1
    autoscaling:
      enabled: false
